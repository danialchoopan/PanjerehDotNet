@page
@model AdminModel
@{ ViewData["Title"] = "پنل مدیریت"; }
<h1 class="text-2xl font-bold">داشبورد مدیریت</h1>
<div class="grid grid-cols-3 gap-4 mt-4">
    <div class="border p-4">کل آگهی‌ها: @Model.Stats.TotalAds</div>
</div>
